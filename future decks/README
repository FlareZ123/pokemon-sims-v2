Do not touch anything in here, these decks are just so that the simulator and agents (such as yourself) do not touch them.
I will move them into the active `main decks` places piece by piece so everything gets simulated eventually.